# test pgm to produce readin/subset/transform/aggregate steps 
# w/small test file

filepath = 
selection_logic = 
variables_keep = 
outputfile_prefix = 'labor_and_delivery'

with open(filepath,buffer=, mode='rt') as file:
    for line, enumerate file:
        count = 
        if count==1:
            headerrow = line (minus single quotes and return char)
            print("Header row:")
            print(headerrow)
        else:



